# LCT Build - Frontend

Фронтенд приложение для системы строительного контроля, разработанное на React + TypeScript + Vite.

## 📋 Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Запуск проекта](#запуск-проекта)
- [Структура проекта](#структура-проекта)
- [Доступные скрипты](#доступные-скрипты)
- [Технологии](#технологии)
- [API Интеграция](#api-интеграция)
- [Роли пользователей](#роли-пользователей)

## 🔧 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** версии 16.x или выше ([Скачать](https://nodejs.org/))
- **npm** версии 8.x или выше (устанавливается вместе с Node.js)
- **Git** ([Скачать](https://git-scm.com/))

Проверить установленные версии:
```bash
node --version
npm --version
git --version
```

## 📦 Установка

### 1. Клонирование репозитория

```bash
cd D:\Code\LCT_build
```

### 2. Переход в директорию фронтенда

```bash
cd lct-front
```

### 3. Установка зависимостей

```bash
npm install
```

Эта команда установит все необходимые пакеты из `package.json`.

## 🚀 Запуск проекта

### Режим разработки

Запустите сервер разработки с горячей перезагрузкой:

```bash
npm run dev
```

После запуска приложение будет доступно по адресу:
- **Локально**: `http://localhost:3000`
- **В сети**: `http://[ваш-IP]:3000`

Сервер автоматически перезагрузит страницу при изменении файлов.

### Сборка для продакшена

Создать оптимизированную сборку:

```bash
npm run build
```

Собранные файлы будут находиться в директории `dist/`.

### Предпросмотр продакшен сборки

Локальный просмотр продакшен сборки:

```bash
npm run preview
```

## 📁 Структура проекта

```
lct-front/
├── public/                 # Статические файлы
│   └── logo.png           # Favicon
├── src/
│   ├── assets/            # Ресурсы (изображения, иконки)
│   │   ├── icons/         # SVG и PNG иконки
│   │   ├── images/        # Изображения
│   │   └── fonts/         # Шрифты
│   ├── components/        # Переиспользуемые компоненты
│   │   ├── ui/           # UI компоненты (Button, Input, Card, Modal)
│   │   ├── layout/       # Layout компоненты (Header, Sidebar, DashboardLayout)
│   │   ├── features/     # Функциональные компоненты (LoginForm)
│   │   └── ProjectCard/  # Карточка проекта
│   ├── pages/            # Страницы приложения
│   │   ├── Auth/         # Аутентификация (Login, Register)
│   │   ├── Projects/     # Страница проектов
│   │   ├── Landing/      # Посадочная страница
│   │   └── ...
│   ├── hooks/            # Custom React hooks
│   │   └── useAuth.ts    # Хук аутентификации
│   ├── services/         # API сервисы
│   │   └── api/
│   │       └── auth.ts   # Сервис аутентификации
│   ├── types/            # TypeScript типы
│   │   ├── auth.ts       # Типы для аутентификации
│   │   ├── objects.ts    # Типы для объектов
│   │   └── ...
│   ├── utils/            # Утилиты
│   │   ├── constants.ts  # Константы
│   │   └── helpers.ts    # Вспомогательные функции
│   ├── styles/           # Глобальные стили
│   │   ├── globals.css   # Глобальные стили
│   │   ├── variables.css # CSS переменные
│   │   └── fonts.css     # Подключение шрифтов
│   ├── App.tsx           # Главный компонент
│   ├── main.tsx          # Точка входа
│   └── vite-env.d.ts     # Типы для Vite
├── .env                   # Переменные окружения
├── index.html            # HTML шаблон
├── package.json          # Зависимости и скрипты
├── tsconfig.json         # Конфигурация TypeScript
├── vite.config.ts        # Конфигурация Vite
└── README.md             # Этот файл
```

## 📜 Доступные скрипты

В директории проекта вы можете выполнить:

### `npm run dev`
Запускает приложение в режиме разработки на порту 3000.

### `npm run build`
Собирает приложение для продакшена в папку `dist/`.

### `npm run preview`
Локальный просмотр продакшен сборки.

### `npm run lint`
Проверяет код на соответствие стандартам ESLint.

## 🛠 Технологии

### Основные
- **React** 18.x - UI библиотека
- **TypeScript** 5.x - Типизация
- **Vite** 5.x - Сборщик и dev-сервер

### Стили
- **CSS Modules** - Модульные стили
- **Google Fonts** - Шрифт Manrope

### Утилиты
- **Path Aliases** - Удобные импорты (`@/components`, `@/pages`, и т.д.)

## 🌐 API Интеграция

### Конфигурация

API URL настраивается в файле `.env`:

```env
VITE_API_URL=https://lct-back.kixylab.ru
```

### Эндпоинты

**Регистрация**:
```
POST https://lct-back.kixylab.ru/sign/up
```

**Вход**:
```
POST https://lct-back.kixylab.ru/sign/in
```

### Требования к данным регистрации

- **firstName**: 2-24 символа
- **lastName**: 2-24 символа
- **phone**: формат `+79XXXXXXXXX`
- **password**: 6-24 символа (включает заглавную букву, строчную букву, цифру, спецсимвол)
- **role**: 0-3 (см. роли ниже)

## 👥 Роли пользователей

| Код | Название | Описание |
|-----|----------|----------|
| 0 | CUSTOMER | Заказчик |
| 1 | FOREMAN | Прораб |
| 2 | INSPECTOR | Инспектор контрольного органа |
| 3 | ADMIN | Администратор |

## 🔐 Аутентификация

После успешного входа/регистрации JWT токен сохраняется в `localStorage`:

```javascript
localStorage.getItem('jwt')
```

Токен автоматически используется для авторизованных запросов к API.

## 📱 Адаптивность

Приложение полностью адаптировано для различных устройств:

- **Desktop**: 1920px и выше
- **Tablet**: 768px - 1919px
- **Mobile**: до 767px

## 🎨 Цветовая схема

Основные цвета приложения:

- **Primary**: `#7B58FD` (Фиолетовый)
- **Primary Hover**: `#6344E8`
- **Background**: `#F3F3F3` (Светло-серый)
- **Text Primary**: `#1F2937` (Темно-серый)
- **Text Secondary**: `#6B7280` (Серый)
- **White**: `#FFFFFF`

## 🐛 Решение проблем

### Порт уже занят

Если порт 3000 занят, измените его в `vite.config.ts`:

```typescript
server: {
  port: 3001, // Измените на свободный порт
  host: true,
}
```

### Ошибки при установке зависимостей

Попробуйте очистить кэш и переустановить:

```bash
rm -rf node_modules
rm package-lock.json
npm cache clean --force
npm install
```

### Проблемы с CORS

Убедитесь, что бэкенд разрешает запросы с вашего домена.

## 📝 Примеры использования

### Тестовые данные для входа

```json
{
  "phone": "+79123456789",
  "password": "Password123!"
}
```

### Тестовые данные для регистрации

```json
{
  "firstName": "Иван",
  "lastName": "Иванов",
  "phone": "+79123456789",
  "password": "Password123!",
  "role": 0
}
```

## 🤝 Контрибьюция

1. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
2. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
3. Push в branch (`git push origin feature/AmazingFeature`)
4. Откройте Pull Request

## 📄 Лицензия

Проект разработан для Moscow City Hackathon "Leaders of Digital Transformation 2024".

## 📞 Поддержка

При возникновении проблем:

1. Проверьте [Issues](https://github.com/your-repo/issues)
2. Создайте новый Issue с подробным описанием проблемы
3. Приложите логи из консоли браузера и терминала

---

**Дата создания**: Октябрь 2024  
**Версия**: 1.0.0  
**Статус**: В разработке
